const fs = require('fs'); const cheerio = require('cheerio'); const html = fs.readFileSync('minutouno.html', 'utf8'); const $ = cheerio.load(html); console.log('OG TITLE:', meta[property=\"og:title\"].attr('content')); console.log('OG IMAGE:', meta[property=\"og:image\"].attr('content')); console.log('BODY:', .article-body.text().substring(0, 200)); console.log('CONTENT:', .detail-body.text().substring(0, 200));  
